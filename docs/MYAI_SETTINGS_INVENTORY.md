<!-- AUTO-GENERATED by npm run docs:settings-inventory — do not edit by hand -->
# `myAi.*` settings inventory

Generated from `package.json` `contributes.configuration.properties`. Regenerate after changing contributed settings:

```bash
cd autonomous_factory
npm run docs:settings-inventory
```

## Table

| Key | Type | Default | Description |
| --- | --- | --- | --- |
| `myAi.agents.enforceDefaultCodingStandards` | boolean | `true` | Inject shared modularity / structure guidance into planner (non-blueprint), implementer, reviewer, and architect prompts. |
| `myAi.agents.goalFirstDiscipline` | boolean | `true` | Inject goal-first execution discipline into all mission agents (system + user tool prompt): clarify end-state, compare viable approaches before non-trivial implementation, then execute. Operators do not need to repeat this in mission text. |
| `myAi.agents.lazyToolPrompt` | boolean | `false` | Use a shorter TOOL cheat sheet in the user prompt; agents should call listTools and listMcpTools to discover git, docker, http, webSearch, and other builtins. Saves context tokens. |
| `myAi.agents.maxPromptChars` | number | `60000` | Maximum characters for the prompt sent to the LLM. Sections are progressively trimmed when exceeding 80% of this budget. |
| `myAi.agents.modelMap` | string | `` | Optional per-role model mapping, e.g. planner:gpt-4.1,researcher:gpt-4.1,implementer:llama3.1 |
| `myAi.agents.providerMap` | string | `` | Optional per-role provider mapping, e.g. planner:openai,researcher:vscode-lm,implementer:ollama |
| `myAi.agents.routingPreset` | string | `default` | Default agent routing preset for new missions. |
| `myAi.anthropic.baseUrl` | string | `https://api.anthropic.com/v1` | Anthropic API root. |
| `myAi.approvals.bundleAutoSummary` | boolean | `true` | Enable summary documents for approval bundles. |
| `myAi.browser.captureCommand` | string | `` | Shell command template with {url} and {outPath} (e.g. npx playwright screenshot {url} {outPath}). Values are single-quoted. Requires Playwright or similar on PATH. |
| `myAi.browser.captureFileExtension` | string | `png` | File extension for capture output under diskStoreFolder/browser-captures/. |
| `myAi.browser.captureMaxOutputBytes` | number | `65536` | Max stdout+stderr bytes captured for browserCapture. |
| `myAi.browser.captureTimeoutMs` | number | `120000` | Timeout for browserCapture command (ms). |
| `myAi.browser.enabled` | boolean | `false` | Enable browserCapture tool (runs myAi.browser.captureCommand; no browser bundled in the extension). |
| `myAi.context.relevantSnippetsCacheTtlMs` | number | `30000` | TTL (ms) for caching keyword→relevant-file snippets in EnhancedContextCollector; 0 disables cache. |
| `myAi.defaultModel` | string | `llama3.1` | Legacy global default model name. Prefer myAi.models.<provider> for coherent per-provider defaults. |
| `myAi.defaultProvider` | string | `ollama` | Default model provider. |
| `myAi.embeddings.model` | string | `text-embedding-3-small` | Model name for embedding requests (OpenAI-compatible providers). |
| `myAi.embeddings.ollamaModel` | string | `nomic-embed-text` | Model name for Ollama embedding requests. |
| `myAi.embeddings.provider` | string | `openai` | Provider to use for real embedding requests. |
| `myAi.embeddings.useRealEmbeddings` | boolean | `false` | Use real embedding models for semantic memory search. Falls back to local FNV-1a hashing when disabled or unavailable. |
| `myAi.gemini.baseUrl` | string | `https://generativelanguage.googleapis.com` | Google Generative Language API root (non-secret). |
| `myAi.index.buildOnActivation` | boolean | `false` | When true, scan and index workspace files as soon as the extension activates. When false (default), indexing starts on first use (e.g. findRelevantFiles) to speed up startup. |
| `myAi.index.excludeGlobs` | string | `**/node_modules/**,**/dist/**,**/.git/**,**/vendor/**,**/__pycache__/**` | Comma-separated glob patterns to exclude from the workspace index. |
| `myAi.index.includeGlobs` | string | `**/*.{ts,js,py,go,rs,java,tsx,jsx,vue,rb,cs,cpp,c,h,hpp}` | Glob patterns for files to include in the workspace index. |
| `myAi.index.incrementalSaveDebounceMs` | number | `2000` | Debounce window (ms) for re-indexing a file after save. Coalesces rapid saves. |
| `myAi.index.maxFiles` | number | `500` | Maximum number of files to include in the workspace semantic index. |
| `myAi.mcp.configPath` | string | `examples/mcp.sample.json` | Workspace-relative or absolute path to MCP server config JSON. |
| `myAi.mcp.persistSessions` | boolean | `true` | Keep MCP stdio servers alive across calls instead of spawning per request. |
| `myAi.mcp.sessionWarmupOnStartup` | boolean | `false` | Warm configured MCP sessions on startup. |
| `myAi.mcp.sessionWarmupStaggerMs` | number | `0` | When sessionWarmupOnStartup is true, wait this many milliseconds between starting each server (0 = no delay). Spreads process spawn load. |
| `myAi.mcp.toolCacheTtlSeconds` | number | `60` | How long to cache MCP tool descriptors per live session. |
| `myAi.memory.enableGlobalSemanticMemory` | boolean | `true` | Persist memory across missions and use it for semantic recall. |
| `myAi.memory.globalRecallLimit` | number | `6` | Maximum global semantic memory items recalled into an agent turn. |
| `myAi.memory.maxRecentEvents` | number | `250` | Maximum recent memory events stored per mission. |
| `myAi.memory.minSemanticScore` | number | `0.11` | Minimum semantic similarity score for memory recall results. |
| `myAi.memory.semanticRecallLimit` | number | `8` | Maximum semantic memory items recalled into an agent turn. |
| `myAi.missions.adaptiveFailurePenaltyRounds` | number | `4` | In adaptive scaling, subtract this many effective rounds per failed or dead-letter queue row. |
| `myAi.missions.adaptiveLoopGuardPenaltyRounds` | number | `8` | In adaptive scaling, subtract this many effective rounds per runtime.loopGuardTrips (stall/max-round trips). |
| `myAi.missions.adaptiveMaxRounds` | number | `200` | Hard cap for adaptive scaling mode. |
| `myAi.missions.architectPassAfterValidator` | boolean | `false` | When true and blueprint is approved, enqueue an architect gap review after each validator COMPLETE. |
| `myAi.missions.autoCheckpointEveryStep` | boolean | `true` | Persist a checkpoint after each step. |
| `myAi.missions.autoResumeOnStartup` | boolean | `true` | Resume running or queued missions on startup. |
| `myAi.missions.blueprintFidelityCheck` | boolean | `false` | When true, after each successful implementer step on an approved blueprint mission, flush file tracker and enqueue an optional reviewer if modified paths do not match blueprint-derived keywords (heuristic; allowlists common config files). |
| `myAi.missions.blueprintMode` | boolean | `false` | When true, new missions start with a full JSON blueprint planner pass; approve before synthesized work runs. |
| `myAi.missions.claimDiscipline` | boolean | `true` | Inject claim-discipline instructions so agents can record VERIFIED_* vs ASSUMPTION via MEMORY lines. |
| `myAi.missions.closureRequired` | boolean | `true` | Keep missions alive until validation passes or a real blocker is recorded. |
| `myAi.missions.diskStoreFolder` | string | `.my-ai-extension` | Workspace-relative folder for mission files and approval previews. |
| `myAi.missions.gitCheckpointBeforeImpl` | boolean | `false` | Automatically git-stash before implementer work items and restore on failure. Requires a git repository in the workspace. |
| `myAi.missions.heartbeatSeconds` | number | `12` | Background runner heartbeat interval in seconds. |
| `myAi.missions.markDeadLetterAfterRetryExhaustion` | boolean | `true` | When automatic retries are exhausted (or max is 0), mark the failed work item as dead letter, append operator guidance to output, and emit an error event. |
| `myAi.missions.maxAutoRetries` | number | `2` | Maximum automatic retries for failed work items. Set 0 to disable. Retries inject error context so the agent can try a different approach. |
| `myAi.missions.maxAutoRounds` | number | `24` | Maximum autonomous rounds before the mission is paused as blocked for operator review. |
| `myAi.missions.maxBlueprintRevisions` | number | `3` | Max operator-requested blueprint revision rounds per mission. |
| `myAi.missions.maxBlueprintSteps` | number | `40` | Maximum steps accepted in a parsed mission blueprint. |
| `myAi.missions.maxStallAutoReplans` | number | `2` | Maximum automatic recovery replans before a mission is marked blocked for operator review. |
| `myAi.missions.maxStepsPerRun` | number | `16` | Safety cap for autonomous steps in one run. |
| `myAi.missions.maxToolFollowUpTurns` | number | `3` | Maximum number of follow-up turns where tool results are fed back to the agent for iterative refinement. Set to 0 to disable tool follow-up loops. |
| `myAi.missions.minCompletedWorkItems` | number | `4` | Minimum completed work items before a closure-required mission may complete. |
| `myAi.missions.pauseAfterEachValidator` | boolean | `false` | When true, after each validator work item completes successfully, pause the mission (awaiting_input) until the operator runs Autonomous Factory: Resume Mission. |
| `myAi.missions.policyPreset` | string | `balanced` | Default closure policy preset for new missions. |
| `myAi.missions.portableJson` | boolean | `true` | Persist missions as portable JSON files under the workspace folder. |
| `myAi.missions.preBlueprintClarification` | boolean | `false` | When blueprint mode is on, run one planner pass first that outputs structured clarification questions; operator answers in the missions inspector (or command), then blueprint JSON is generated with Q&A in context. |
| `myAi.missions.requireBlueprintApproval` | boolean | `true` | Pause after blueprint generation until operator approves (when blueprintMode is on). |
| `myAi.missions.requireImplementerBeforeComplete` | boolean | `true` | Require at least one implementer step before closure-required missions can complete. |
| `myAi.missions.requirePlannerCoverage` | boolean | `true` | Require planner-generated role coverage (implementer/reviewer/validator) before closure. |
| `myAi.missions.requireReviewerBeforeComplete` | boolean | `true` | Require at least one reviewer step before a closure-required mission completes. |
| `myAi.missions.requireValidationEvidence` | boolean | `true` | Require explicit validation evidence before a closure-required mission can complete. |
| `myAi.missions.requireValidatorBeforeComplete` | boolean | `true` | Require validator sign-off before a closure-required mission completes. |
| `myAi.missions.scalingMode` | string | `fixed` | How mission step/round limits scale. 'fixed' uses config values directly. 'adaptive' scales limits based on work item count. |
| `myAi.missions.stallHeartbeatThreshold` | number | `3` | Number of heartbeats with no progress before the runner injects a recovery replan. |
| `myAi.missions.stallReplanThreshold` | number | `3` | If too few todo items remain before closure, enqueue an automatic re-plan after this many rounds without progress. |
| `myAi.missions.toolResultSpill.enabled` | boolean | `true` | When true, large tool result payloads in mission events are written under the disk store folder and referenced inline to keep mission JSON smaller. |
| `myAi.missions.toolResultSpill.maxInlineBytes` | number | `24000` | If serialized event data exceeds this UTF-8 byte size, spill to disk (when enabled and a workspace folder exists). |
| `myAi.missions.trustGates.enabled` | boolean | `false` | When true, dependency-style shell commands and very large patches (after many file touches) require operator approval unless a recent verified MEMORY claim exists. |
| `myAi.missions.trustGates.largePatchMinChars` | number | `8000` | Minimum search+replace character total to treat an applyPatch as 'large' for trust gating (with many files touched). |
| `myAi.missions.trustGates.manyModifiedFilesThreshold` | number | `12` | Mission filesModified count at or above which large patches trigger the trust gate (when enabled). |
| `myAi.missions.verification.autoRunLinterAfterMutations` | boolean | `true` | When true, balanced/strict mission policy automatically runs the linter after implementer mutations (Verifier Mesh obligation). |
| `myAi.missions.verification.autoRunTestsAfterMutations` | boolean | `true` | When true, balanced/strict mission policy automatically runs tests after implementer mutations (Verifier Mesh obligation). |
| `myAi.models.anthropic` | string | `claude-3-5-sonnet-20240620` | Default Claude model id for the Anthropic provider. |
| `myAi.models.gemini` | string | `gemini-1.5-flash` | Default Gemini model id. |
| `myAi.models.ollama` | string | `llama3.1` | Default model when the Ollama provider is selected. |
| `myAi.models.openai` | string | `gpt-4.1-mini` | Default model when the OpenAI provider is selected. |
| `myAi.models.openaiCompat` | string | `Qwen/Qwen2.5-7B-Instruct` | Default model id for local OpenAI-compatible (vLLM, etc.) endpoints. |
| `myAi.models.vscodeLm` | string | `` | Optional hint to match a VS Code LM id/family when multiple models exist. |
| `myAi.ollama.baseUrl` | string | `http://127.0.0.1:11434` | Ollama base URL. |
| `myAi.openai.baseUrl` | string | `https://api.openai.com/v1` | OpenAI-compatible base URL. |
| `myAi.openaiCompat.baseUrl` | string | `http://127.0.0.1:8000/v1` | Base URL for local OpenAI-compatible servers (vLLM, llama.cpp server, etc.). |
| `myAi.providers.maxRetries` | number | `1` | Maximum retry attempts for transient provider failures. |
| `myAi.providers.requestTimeoutMs` | number | `30000` | Provider request timeout in milliseconds. |
| `myAi.providers.retryDelayMs` | number | `400` | Base delay in milliseconds between provider retries. |
| `myAi.reviewers.autoCreateFixTasks` | boolean | `true` | When reviewer output reports issues without an implementer follow-up, automatically inject remediation/re-review/re-validation tasks. |
| `myAi.sendActiveFile` | boolean | `true` |  |
| `myAi.sendDiagnostics` | boolean | `true` |  |
| `myAi.sendSelection` | boolean | `true` |  |
| `myAi.skills.enabled` | boolean | `true` | Load markdown from myAi.skills.globPatterns and inject into agent system prompts (bounded size). |
| `myAi.skills.globPatterns` | array | `[".my-ai/skills/**/*.md","docs/agent-skills/**/*.md"]` | Workspace-relative globs for skill markdown files. |
| `myAi.skills.maxCharsPerFile` | number | `4000` | Maximum characters read from each skill file. |
| `myAi.skills.maxTotalChars` | number | `12000` | Maximum total characters injected from all skill files per agent turn. |
| `myAi.tools.allowExternalAdapters` | boolean | `true` | Allow external HTTP-backed tool adapters. |
| `myAi.tools.allowTerminal` | boolean | `false` | Allow terminal tool execution. |
| `myAi.tools.commandOutputMaxBytes` | number | `8192` | Maximum bytes of stdout/stderr captured per runCommand invocation. Output beyond this limit is truncated. |
| `myAi.tools.commandTimeoutMs` | number | `30000` | Default timeout in milliseconds for runCommand tool calls. Individual calls can override this. |
| `myAi.tools.externalAdaptersFile` | string | `.my-ai-extension/tools/adapters.json` | Workspace-relative JSON file defining external tool adapters. |
| `myAi.tools.listMcpToolsSummaryMaxChars` | number | `0` | When > 0, listMcpTools strips inputSchema, adds inputPropertyNames from schema properties when available, trims descriptions, and fits JSON.stringify(data) to this budget; may drop trailing tools and append __myAi__.list_budget. 0 = full MCP descriptors (legacy). |
| `myAi.tools.listToolsHintsMaxChars` | number | `16000` | Maximum characters for the listTools `hints` array (one-line usage per tool). Set to 0 to omit hints and keep only builtin names + external adapters. |
| `myAi.tools.listToolsRedactExternalUrls` | boolean | `false` | When true, listTools omits url and headers from the `external` array (and sets externalUrlsRedacted). Hints still work; use adapters.json on disk for full URLs. |
| `myAi.tools.requireApprovalForExternal` | boolean | `true` | Require approval before calling a mutating external tool adapter. |
| `myAi.tools.requireApprovalForHttp` | boolean | `true` | Require approval before httpRequest tool calls. |
| `myAi.tools.requireApprovalForInWorkspaceWrites` | boolean | `true` | When true (default), workspace-path writeFile/applyPatch also require approval. Set false to allow autonomous in-workspace writes while restrictToWorkspace keeps paths bounded. |
| `myAi.tools.requireApprovalForMcp` | boolean | `true` | Require approval before MCP tool calls. |
| `myAi.tools.requireApprovalForTerminal` | boolean | `true` | Require approval before runTerminal tool calls. |
| `myAi.tools.requireApprovalForWrite` | boolean | `true` | When true, mutating writeFile/applyPatch calls require operator approval before the tool runs (subject to requireApprovalForInWorkspaceWrites for paths inside the workspace). |
| `myAi.tools.restrictToWorkspace` | boolean | `true` | Restrict file tools to workspace-contained paths. |
| `myAi.tools.searchFilesMaxResults` | number | `50` | Max ripgrep matches per searchFiles / grepSearch tool call (agent requests are capped to this). |
| `myAi.tools.showDiffBeforeApproval` | boolean | `true` | Open a diff preview before approving writeFile/applyPatch actions. |
| `myAi.trace.level` | string | `info` | Maximum verbosity for the unified Autonomous Factory Trace output channel. error = errors only; info = operator-friendly; debug/trace = more host/webview detail. |
| `myAi.trace.persistToFile` | boolean | `false` | Append JSON trace lines to globalStorage/trace/my-ai-trace-append.jsonl (in addition to the output channel). |
| `myAi.ui.autoRevealOnActivation` | boolean | `false` | Automatically reveal the Autonomous Factory sidebar when the extension activates. |
| `myAi.ui.dashboardPollIntervalMs` | number | `25000` | Interval for automatic sidebar snapshot refresh while the webview is visible. Lower values increase extension-host CPU, disk, and MCP traffic. |
| `myAi.ui.defaultTab` | string | `chat` | Default tab shown when the Autonomous Factory sidebar loads. |
| `myAi.ui.retainWebviewContextWhenHidden` | boolean | `true` | Keep the sidebar webview JavaScript heap when the view is hidden (smoother tab switches; more memory). Set false to reduce memory at the cost of full reload when reopening. |
| `myAi.ui.traceAutoRefreshIntervalMs` | number | `10000` | Interval (ms) for Trace tab auto-refresh when enabled. Higher values reduce host/webview traffic. |
| `myAi.useNativeChatParticipant` | boolean | `false` | Register a native VS Code chat participant when available. |
| `myAi.webResearch.cacheEnabled` | boolean | `true` | Cache successful webSearch/fetchWebPage results within a mission to reduce repeated network calls. |
| `myAi.webResearch.cacheTtlMs` | number | `600000` | Time-to-live for cached webSearch/fetchWebPage results (ms). When stale, the tool refetches and logs a warning. |
| `myAi.webResearch.enabled` | boolean | `false` | Enable webSearch and fetchWebPage tools (HTTP; uses same approval policy as httpRequest). |
| `myAi.webResearch.localLlmProviderIds` | string | `ollama` | Comma/space-separated mission provider ids (activeProviderId when the mission starts) treated as “local LLM” for unlimitedBudgetForLocalLlm. Default: ollama. |
| `myAi.webResearch.maxCallsPerMission` | number | `0` | Maximum successful webSearch/fetchWebPage network calls per mission (0 = unlimited). When exceeded, the tool fails with guidance to reuse memory/cache instead of operator approval. |
| `myAi.webResearch.skipHttpApproval` | boolean | `true` | When true, webSearch and fetchWebPage bypass the global myAi.tools.requireApprovalForHttp gate (role envelopes and other policy still apply). The generic httpRequest tool is unchanged. |
| `myAi.webResearch.unlimitedBudgetForLocalLlm` | boolean | `true` | When true and the provider for the current work item resolves to local (workItem.providerId, else routing.providerPerRole[role], else mission active provider; see localLlmProviderIds), the extension skips paid-API-style web limits: webSearch/fetchWebPage call caps and webSearch minIntervalMs. Use mixed routing (e.g. planner cloud + implementer local) and attributed tool calls (__workItemId) so each role is judged correctly. |
| `myAi.webResearch.warnStaleEvidenceOnMutation` | boolean | `true` | When true (balanced/strict missions), warn if mission memory still contains web research evidence past its freshness TTL when an implementer performs a mutating change. |
| `myAi.webSearch.minIntervalMs` | number | `0` | Minimum milliseconds between webSearch HTTP requests after approval (0 = disabled). Reduces load on the search API. |
| `myAi.webSearch.provider` | string | `duckduckgo` | webSearch backend: duckduckgo (instant answer API; fair use) or brave (Brave Search API). For brave, use Command Palette → Autonomous Factory: Set Brave Search API Key (stores myAi.webSearch.braveApiKey in Secret Storage). |

## Finding code references

Contributed keys use dotted form. To find where a setting is **read** in this extension:

```bash
rg 'myAi\\.missions\\.portableJson' src
# or broader:
rg 'getConfiguration\\(' src | head -80
```

This table does **not** list every callsite — only defaults and descriptions from `package.json`.
